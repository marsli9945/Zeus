server:
  port: 8004
spring:
  application:
    name: admin
  data:
    mongodb:
      uri: mongodb://oauth:123456@127.0.0.1:27017/oauth
  jpa:
    hibernate:
      ddl-auto: update # 更新或创建数据表
    show-sql: true # 打印sql
    open-in-view: true
