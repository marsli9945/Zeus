server:
  port: 8761
eureka:
  instance:
    hostname: eureka #eureka实例
  client:
    register-with-eureka: false #不把自己注册进来
    fetch-registry: false #不在注册中心获取注册信息
    service-url:
      defaultZone: http://localhost:8761/eureka/
